@model GamersHub.Web.ViewModels.Replies.ReplyDeleteViewModel
@{
    ViewData["Title"] = "Delete Reply";
    
}

<partial name="_RepliesPartial" model="Model.Post" view-data="ViewData"/>

<div class="card">
    <div class="card-header">
        <h5 class="d-inline">@Model.UserUsername</h5>
        <h5 class="float-right">User Publications: @Model.Publications</h5>
    </div>
    <div class="card-body">
        <p class="card-text">@Html.Raw(Model.SanitizedContent)</p>
    </div>
    <div class="card-footer">
        <span class="d-inline"><i class="fas fa-calendar-alt"></i> Posted on: <time datetime="@Model.CreatedOn"></time></span>
        @if (Model.ModifiedOn != null)
        {
            <span class="float-right"><i class="fas fa-calendar-alt"></i> Last Modified: <time datetime="@Model.ModifiedOn"></time></span>
        }
    </div>
</div>

<div class="d-block">
    <h2>Are you sure you want to Delete this reply ?</h2>
</div>

<form class="d-inline-block m-1" asp-controller="Replies" asp-action="Delete" method="post">
    <div class="form-group">
        <input type="hidden" name="PostId" value="@Model.Post.Id"/>
    </div>
    <div class="form-group">
        <input type="hidden" name="PostUrl" value="@Model.Post.Url"/>
    </div>
    <div class="form-group">
        <input type="submit" value="Delete" class="btn btn-danger btn-lg"/>
    </div>
</form>

<div class="d-inline-block m-1">
    <a asp-controller="Posts" asp-action="@Model.Post.Url" asp-route-id="@Model.Post.Id" type="submit" class="btn btn-primary btn-lg">Cancel</a>
</div>